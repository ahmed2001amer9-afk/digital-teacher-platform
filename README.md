# Digital Teacher Platform (Next.js + Tailwind)

Run locally:

```
npm install
npm run dev
```

Deploy to Vercel:
1) Push this folder to a GitHub repo.
2) On vercel.com → New Project → Import GitHub → Deploy.
